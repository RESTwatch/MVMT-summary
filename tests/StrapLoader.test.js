import React from 'react';
import { shallow } from 'enzyme';

import StrapLoader from '../client/src/components/StrapLoader';

describe('StrapLoader component', () => {
  it('should be defined', () => {
    expect(StrapLoader).toBeDefined();
  });
});
